package xyz.jovanstoiljkovic.isa.repositories;import org.springframework.data.jpa.repository.Query;import org.springframework.stereotype.Repository;import xyz.jovanstoiljkovic.isa.models.Song;import java.util.List;@Repositorypublic interface SongRepository extends BaseRepository<Song, Long> {    //TODO: null check breaking the entire query, not having it breaks it also...    /*    @Query( "SELECT s " +            "FROM Song s " +            "WHERE s.name LIKE %?1%" +            "OR (s.album IS NOT NULL AND s.album.name LIKE %?1%)")  +            "OR s.artist.name LIKE %?1%")    List<Song> search(String search);*/}